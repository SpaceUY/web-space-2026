---
import BaseLayout from "./BaseLayout.astro";
import Button from "../components/ui/Button.astro";
import Badge from "../components/ui/Badge.astro";
import SectionHeading from "../components/ui/SectionHeading.astro";
import FAQ from "../components/ui/FAQ.astro";
import CTA from "../components/ui/CTA.astro";
import CaseStudyCard from "../components/ui/CaseStudyCard.astro";
import Breadcrumbs from "../components/seo/Breadcrumbs.astro";
import { type ServicePageData, getCaseStudies } from "../data/services";
import QuoteBlock from "../components/ui/QuoteBlock.astro";

const techIconMap: Record<string, { label: string; svg: string }> = {
  ethereum: {
    label: "Ethereum / EVM",
    svg: `<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M16 4L6.5 16.5L16 13.5L25.5 16.5L16 4Z" fill="white" opacity="0.9"/>
      <path d="M6.5 16.5L16 21.5L25.5 16.5L16 13.5L6.5 16.5Z" fill="white" opacity="0.5"/>
      <path d="M16 23L6.5 18L16 28L25.5 18L16 23Z" fill="white" opacity="0.85"/>
    </svg>`,
  },
  solana: {
    label: "Solana",
    svg: `<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M7 9h15l-3 3H7V9Z" fill="white" opacity="0.95"/>
      <path d="M7 14.5h15l-3 3H7v-3Z" fill="white" opacity="0.75"/>
      <path d="M10 20h15l-3 3H10v-3Z" fill="white" opacity="0.55"/>
    </svg>`,
  },
  solidity: {
    label: "Solidity",
    svg: `<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
      <polygon points="16,4 26,12 22,24 10,24 6,12" stroke="white" stroke-width="1.5" fill="none" opacity="0.9"/>
      <polygon points="16,9 21,13 19,20 13,20 11,13" fill="white" opacity="0.6"/>
      <path d="M13 8L10 4M19 8L22 4" stroke="white" stroke-width="1.2" stroke-linecap="round"/>
    </svg>`,
  },
  rust: {
    label: "Rust",
    svg: `<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
      <circle cx="16" cy="16" r="10" stroke="white" stroke-width="1.5"/>
      <circle cx="16" cy="16" r="4" stroke="white" stroke-width="1.2"/>
      <rect x="15.2" y="5.5" width="1.6" height="3" rx="0.8" fill="white"/>
      <rect x="15.2" y="23.5" width="1.6" height="3" rx="0.8" fill="white"/>
      <rect x="5.5" y="15.2" width="3" height="1.6" rx="0.8" fill="white"/>
      <rect x="23.5" y="15.2" width="3" height="1.6" rx="0.8" fill="white"/>
      <rect x="8.2" y="8.2" width="1.6" height="3" rx="0.8" fill="white" transform="rotate(45 9 9)"/>
      <rect x="21.2" y="8.2" width="1.6" height="3" rx="0.8" fill="white" transform="rotate(-45 22 9)"/>
      <rect x="8.2" y="21.2" width="1.6" height="3" rx="0.8" fill="white" transform="rotate(-45 9 22)"/>
      <rect x="21.2" y="21.2" width="1.6" height="3" rx="0.8" fill="white" transform="rotate(45 22 22)"/>
      <path d="M13 12h3.5c1.4 0 2.5 1 2.5 2.2 0 1-0.7 1.9-1.7 2.1L19 20h-2l-1.5-3.5H14.5V20H13V12Z" fill="white"/>
    </svg>`,
  },
  wagmi: {
    label: "Wagmi",
    svg: `<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
      <rect x="5" y="18" width="4" height="8" rx="1.5" fill="white" opacity="0.6"/>
      <rect x="11" y="13" width="4" height="13" rx="1.5" fill="white" opacity="0.8"/>
      <rect x="17" y="8" width="4" height="18" rx="1.5" fill="white"/>
      <rect x="23" y="13" width="4" height="13" rx="1.5" fill="white" opacity="0.8"/>
      <circle cx="26" cy="10" r="1.5" fill="white"/>
    </svg>`,
  },
  hardhat: {
    label: "Hardhat",
    svg: `<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M6 20h20v2.5c0 .8-.7 1.5-1.5 1.5h-17C6.7 24 6 23.3 6 22.5V20Z" fill="white" opacity="0.8"/>
      <path d="M8 20c0-4.4 3.6-8 8-8s8 3.6 8 8H8Z" fill="white" opacity="0.95"/>
      <path d="M14 8.5V12M18 8.5V12" stroke="white" stroke-width="1.2" stroke-linecap="round"/>
      <path d="M13 8a3 3 0 016 0" stroke="white" stroke-width="1.2" fill="none"/>
    </svg>`,
  },
  ethers: {
    label: "Ethers.js",
    svg: `<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M7 20c0-3.3 2.7-6 6-6h1c.3-3 2.8-5.5 6-5.5 3.3 0 6 2.7 6 6 0 .3 0 .6-.1.9C27.5 16 29 17.7 29 19.7 29 22 27 24 24.6 24H8.5C7.7 24 7 23.3 7 22.5V20Z" stroke="white" stroke-width="1.5" fill="none" opacity="0.9"/>
      <path d="M9 27c0-1.7 1.3-3 3-3" stroke="white" stroke-width="1.2" stroke-linecap="round" opacity="0.6"/>
    </svg>`,
  },
  openzeppelin: {
    label: "OpenZeppelin",
    svg: `<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M7 10h18L11 22h14" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>`,
  },
};
import { SITE } from "../lib/seo";

interface Props {
  data: ServicePageData;
}

const { data } = Astro.props;
const featured = data.caseStudySlugs ? getCaseStudies(data.caseStudySlugs) : [];

// Build breadcrumbs from URL path: /a/b/c → Home > A > B > C
const segments = Astro.url.pathname.split("/").filter(Boolean);
const isSubPage = segments.length > 1;
const crumbs = [{ label: "Home", href: "/" }];
let acc = "";
segments.forEach((seg) => {
  acc += "/" + seg;
  const label = seg
    .split("-")
    .map((w) => w[0].toUpperCase() + w.slice(1))
    .join(" ");
  crumbs.push({ label, href: acc });
});

const serviceJsonLd = {
  "@context": "https://schema.org",
  "@type": "Service",
  name: data.title,
  description: data.description,
  provider: { "@type": "Organization", name: SITE.name, url: SITE.url },
  areaServed: "Worldwide",
};
---

<BaseLayout
  title={data.seo.title}
  description={data.seo.description}
  jsonLd={serviceJsonLd}
>
  <!-- HERO -->
  <section class="relative overflow-hidden" style="background: var(--sd-gradient-hero);">
    <div class="absolute inset-0 opacity-25" style="background-image: radial-gradient(circle at 20% 30%, var(--sd-purple), transparent 50%), radial-gradient(circle at 80% 70%, var(--sd-purple-300), transparent 40%);"></div>
    <div class="relative mx-auto max-w-[1440px] px-6 md:px-10 py-24 md:py-32">
      <div class={data.heroImage ? "grid lg:grid-cols-[1.2fr_1fr] gap-12 items-center" : ""}>
        <div class="max-w-4xl" data-animate>
          <Breadcrumbs items={crumbs} visible={false} class="mb-8" />
          <h1 class="text-4xl md:text-6xl lg:text-7xl text-white">{data.title}</h1>
          <p class="mt-8 text-lg md:text-xl text-sd-text-muted max-w-3xl leading-relaxed">{data.description}</p>
          {(data.primaryCta || data.secondaryCta) && (
            <div class="mt-10 space-y-4">
              <div class="flex flex-wrap gap-4 items-center">
                {data.primaryCta && (
                  <Button href={data.primaryCta.href} variant="primary" size="lg" external={data.primaryCta.external}>
                    {data.primaryCta.label}
                  </Button>
                )}
                {data.secondaryCta && (
                  <Button href={data.secondaryCta.href} variant="outline-light" size="lg" external={data.secondaryCta.external}>
                    {data.secondaryCta.label}
                  </Button>
                )}
              </div>
              <!-- Newsletter subscribe -->
              <p class="text-sm text-sd-text-muted">Or subscribe to our newsletter</p>
              <form
                action="https://api.hsforms.com/submissions/v3/integration/submit/22632737/acdc5149-7c32-48f4-9137-ab4212538377"
                method="POST"
                class="flex gap-3 max-w-md"
                onsubmit="return false;"
              >
                <input
                  type="email"
                  name="email"
                  placeholder="Your email"
                  required
                  class="flex-1 rounded-full px-5 py-3 text-sm bg-white/10 border border-white/20 text-white placeholder:text-sd-text-muted focus:outline-none focus:border-white/40 backdrop-blur-sm"
                />
                <button
                  type="submit"
                  class="rounded-full px-6 py-3 text-sm font-medium bg-white text-sd-text-dark hover:bg-white/90 transition-colors shrink-0"
                >
                  Sign up!
                </button>
              </form>
            </div>
          )}
        </div>
        {data.heroImage && (
          <div class="relative" data-animate>
            <div class="rounded-3xl overflow-hidden border border-white/10 bg-white/5 aspect-square flex items-center justify-center p-6">
              <img
                src={data.heroImage.src}
                alt={data.heroImage.alt}
                width={data.heroImage.width ?? 800}
                height={data.heroImage.height ?? 800}
                loading="eager"
                class="w-full h-full object-contain"
              />
            </div>
          </div>
        )}
      </div>
    </div>
  </section>

  <!-- Named slot: inject content between hero and process -->
  <slot name="pre-process" />

  <!-- PROCESS (numbered steps, horizontal flow) -->
  {data.process && (
    <section class="bg-sd-dark py-20 md:py-28 border-b border-white/5">
      <div class="mx-auto max-w-[1440px] px-6 md:px-10">
        <SectionHeading
          eyebrow={data.process.eyebrow}
          title={data.process.title}
          description={data.process.description}
          align="center"
          class="mb-14"
        />
        <ol class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
          {data.process.items.map((step) => (
            <li class="relative rounded-2xl border border-white/10 bg-sd-dark-2 p-6" data-animate>
              <span class="inline-flex items-center justify-center w-12 h-12 rounded-full bg-sd-purple/15 text-sd-purple-300 font-display text-lg mb-5">
                {step.number}
              </span>
              <h3 class="text-lg text-white mb-2">{step.title}</h3>
              <p class="text-sm text-sd-text-muted leading-relaxed">{step.description}</p>
            </li>
          ))}
        </ol>
      </div>
    </section>
  )}

  <!-- OFFERS (what we build) -->
  {data.offers && (
    <section class="bg-sd-dark py-20 md:py-28">
      <div class="mx-auto max-w-[1440px] px-6 md:px-10">
        <SectionHeading
          eyebrow={data.offers.eyebrow}
          title={data.offers.title}
          description={data.offers.description}
          class="mb-14"
        />
        {data.offersIllustration && (
          <div class="mb-14 flex justify-center" data-animate>
            <img
              src={data.offersIllustration.src}
              alt={data.offersIllustration.alt}
              loading="lazy"
              class="max-w-full h-auto max-h-64 object-contain opacity-90"
            />
          </div>
        )}
        {data.offers.style === "cards" ? (
          <div id="cards-cursor-zone" class="relative grid gap-6 md:grid-cols-2 lg:grid-cols-3">
            <!-- Custom cursor -->
            <div id="cards-cursor" class="pointer-events-none fixed z-50 w-16 h-16 rounded-full opacity-0 transition-opacity duration-200" style="background: var(--sd-purple); transform: translate(-50%, -50%); will-change: transform;" />
            {data.offers.items.map((o) => (
              <a
                href={o.href ?? "#"}
                class="service-card group relative flex flex-col justify-between overflow-hidden rounded-2xl border border-white/10 bg-sd-dark-2 p-6 min-h-[160px] transition-all duration-300"
                data-animate
              >
                {/* Purple gradient bg — visible on hover */}
                <div class="service-card-bg absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-300" style="background: linear-gradient(135deg, #4a397c 17%, #191429 105%);" />

                {/* Arrow icon in bordered box */}
                <div class="relative z-10 inline-flex items-center justify-center w-9 h-9 rounded-md border border-white/20 group-hover:border-white/40 transition-colors shrink-0">
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" class="text-white">
                    <path d="M7 17L17 7M17 7H7M17 7v10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                </div>

                {/* Title — bottom */}
                <h3 class="relative z-10 text-xl md:text-2xl text-white font-display leading-snug mt-auto">{o.title}</h3>
              </a>
            ))}
          </div>
        ) : (
          <div class="grid gap-x-8 gap-y-10 md:grid-cols-2 lg:grid-cols-3">
            {data.offers.items.map((o) => (
              <div class="group" data-animate>
                <p class="text-xs font-medium text-sd-purple-300 mb-3 tracking-widest">{o.number}</p>
                <h3 class="text-lg text-white mb-3 group-hover:text-sd-purple-300 transition-colors">{o.title}</h3>
                <p class="text-sm text-sd-text-muted leading-relaxed">{o.description}</p>
              </div>
            ))}
          </div>
        )}
      </div>
    </section>
  )}

  <!-- WEB3 TECH STACK -->
  {data.techStack && (
    <section class="relative bg-sd-dark py-20 md:py-28 border-t border-white/5 overflow-hidden">
      <div class="absolute inset-0 sd-dot-grid"></div>
      <div class="relative mx-auto max-w-[1440px] px-6 md:px-10">
        <div class="text-center max-w-2xl mx-auto mb-16" data-animate>
          <h2 class="text-3xl md:text-5xl text-white font-display mb-5">{data.techStack.title}</h2>
          <p class="text-sd-text-muted text-base md:text-lg leading-relaxed">{data.techStack.subtitle}</p>
        </div>
        <div class="flex flex-col items-center gap-8 md:gap-10">
          {[data.techStack.tools.slice(0, 5), data.techStack.tools.slice(5)].map((row) => (
            <div class="flex flex-wrap justify-center gap-6 md:gap-10">
              {row.map((tool, i) => {
                const item = techIconMap[tool];
                if (!item) return null;
                const floatClass = ["sd-float-1","sd-float-2","sd-float-3","sd-float-4","sd-float-2","sd-float-3","sd-float-1","sd-float-4"][i % 8];
                return (
                  <div class={`flex flex-col items-center gap-3 ${floatClass}`}>
                    <div class="w-16 h-16 md:w-20 md:h-20 rounded-full flex items-center justify-center p-4"
                         style="background: linear-gradient(135deg, #2d1f6e 0%, #1a1040 100%); border: 1px solid rgba(255,255,255,0.1);">
                      <Fragment set:html={item.svg} />
                    </div>
                    <span class="text-[10px] font-ui uppercase tracking-widest text-sd-text-muted text-center leading-tight">{item.label}</span>
                  </div>
                );
              })}
            </div>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- CASE STUDIES -->
  {featured.length > 0 && (
    <section class="py-20 md:py-28" style="background: var(--sd-purple-deep);">
      <div class="mx-auto max-w-[1440px] px-6 md:px-10">
        <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-6 mb-12">
          <SectionHeading
            eyebrow="Selected work"
            title="Get to know real Blockchain success stories"
            description="We craft decentralized products that deliver measurable impact. Take a closer look at how this service has helped teams ship at scale."
          />
          <Button href="/our-work" variant="outline-light">See all work</Button>
        </div>
        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
          {featured.map((c) => (
            <CaseStudyCard caseStudy={c} />
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- MID CTA (between case studies and benefits) -->
  {data.midCta && (
    <section class="bg-sd-dark py-14 md:py-20 border-t border-white/5">
      <div class="mx-auto max-w-[1440px] px-6 md:px-10">
        <div class="relative overflow-hidden rounded-3xl px-10 py-12 md:px-16 md:py-14 flex flex-col md:flex-row md:items-center md:justify-between gap-8"
             style="background: linear-gradient(135deg, #3b2080 0%, #1e1040 60%, #0f0820 100%);">
          <div class="absolute inset-0 opacity-20" style="background-image: radial-gradient(circle at 70% 50%, #7c3aed, transparent 60%);"></div>
          <p class="relative text-2xl md:text-3xl lg:text-4xl text-white font-display leading-snug max-w-xl">{data.midCta.title}</p>
          <a
            href={data.midCta.buttonHref}
            target={data.midCta.external ? "_blank" : undefined}
            rel={data.midCta.external ? "noopener noreferrer" : undefined}
            class="relative shrink-0 inline-flex items-center gap-2 rounded-full border border-white/30 bg-white/10 px-7 py-3 text-sm text-white font-medium hover:bg-white/20 transition-colors backdrop-blur-sm"
          >
            {data.midCta.buttonLabel}
          </a>
        </div>
      </div>
    </section>
  )}

  <!-- Named slot: inject content between case studies/midCTA and benefits -->
  <slot name="pre-benefits" />

  <!-- BENEFITS -->
  {data.benefits && (
    <section class="bg-sd-dark py-20 md:py-28">
      <div class="mx-auto max-w-[1440px] px-6 md:px-10">
        <SectionHeading
          eyebrow={data.benefits.eyebrow}
          title={data.benefits.title}
          description={data.benefits.description}
          align="center"
          class="mb-14"
        />
        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
          {data.benefits.items.map((b) => (
            <div class="rounded-2xl border border-white/10 bg-sd-dark-2 p-6 hover:border-sd-purple/40 transition-colors" data-animate>
              {b.icon && (
                <div class="mb-5 inline-flex items-center justify-center w-12 h-12 rounded-full bg-sd-purple/20">
                  {b.icon === "lock" && <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#a78bfa" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>}
                  {b.icon === "eye" && <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#a78bfa" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>}
                  {b.icon === "bolt" && <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#a78bfa" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>}
                  {b.icon === "shield" && <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#a78bfa" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>}
                  {b.icon === "tag" && <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#a78bfa" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z"/><line x1="7" y1="7" x2="7.01" y2="7"/></svg>}
                  {b.icon === "chain" && <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#a78bfa" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>}
                </div>
              )}
              <h3 class="text-lg text-white mb-3">{b.title}</h3>
              <p class="text-sm text-sd-text-muted leading-relaxed">{b.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}


  <!-- QUOTE -->
  {data.quote && (
    <QuoteBlock
      text={data.quote.text}
      author={data.quote.author}
      role={data.quote.role}
      photo={data.quote.photo}
      linkedin={data.quote.linkedin}
    />
  )}

  <!-- WHY US -->
  {data.whyUs && (
    <section class="py-20 md:py-28" style="background: var(--sd-purple-deep);">
      <div class="mx-auto max-w-[1440px] px-6 md:px-10">
        {data.whyUs.style === "accordion" ? (
          <>
            <SectionHeading
              eyebrow="Why SpaceDev"
              title={data.whyUs.title}
              description={data.whyUs.description}
              tone="light"
              class="mb-14"
            />
            <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4" data-animate>
              {data.whyUs.items.map((w) => (
                <div class="rounded-2xl border border-white/10 bg-sd-dark-2 p-6">
                  <h3 class="text-base font-medium text-white mb-3">{w.title}</h3>
                  <p class="text-sm text-sd-text-muted leading-relaxed">{w.description}</p>
                </div>
              ))}
            </div>
          </>
        ) : (
          <>
            <SectionHeading
              eyebrow="Why SpaceDev"
              title={data.whyUs.title}
              description={data.whyUs.description}
              align="center"
              class="mb-14"
            />
            <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
              {data.whyUs.items.map((w) => (
                <div class="rounded-2xl border border-white/10 bg-sd-dark-2 p-6" data-animate>
                  <h3 class="text-lg text-white mb-3">{w.title}</h3>
                  <p class="text-sm text-sd-text-muted leading-relaxed">{w.description}</p>
                </div>
              ))}
            </div>
          </>
        )}
      </div>
    </section>
  )}

  <!-- FAQ -->
  {data.faqs && data.faqs.length > 0 && (
    <section class="bg-sd-dark py-20 md:py-28 border-t border-white/5">
      <div class="mx-auto max-w-3xl px-6 md:px-10">
        <SectionHeading
          eyebrow="FAQ"
          title="Frequently asked questions"
          align="center"
          class="mb-12"
        />
        <FAQ items={data.faqs} tone="dark" />
      </div>
    </section>
  )}

  <!-- FINAL CTA -->
  {data.finalCta && (
    <CTA
      eyebrow={data.finalCta.eyebrow}
      title={data.finalCta.title}
      description={data.finalCta.description}
      buttons={[
        { label: data.finalCta.buttonLabel, href: data.finalCta.buttonHref, external: data.finalCta.external },
      ]}
    />
  )}

  <!-- MARQUEE TEXT BANNER -->
  {data.marqueeText && (
    <section class="bg-sd-dark py-8 border-t border-white/5 overflow-hidden">
      <div class="sd-marquee-wrapper">
        <div class="sd-marquee flex items-center whitespace-nowrap" style="animation-duration: 30s">
          {[...Array(6)].map(() => (
            <span
              class="text-5xl md:text-7xl lg:text-8xl font-display font-semibold tracking-tight mr-16 select-none"
              style="background: linear-gradient(90deg, #673ae4 0%, #44cdf3 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;"
            >
              {data.marqueeText} &nbsp;
            </span>
          ))}
        </div>
      </div>
    </section>
  )}

  <slot />
</BaseLayout>

<style>
  .sd-dot-grid {
    background-image: radial-gradient(circle, rgba(255,255,255,0.07) 1px, transparent 1px);
    background-size: 28px 28px;
  }
  @keyframes sd-float-a {
    0%, 100% { transform: translateY(0px); }
    50%       { transform: translateY(-10px); }
  }
  @keyframes sd-float-b {
    0%, 100% { transform: translateY(0px); }
    50%       { transform: translateY(-7px); }
  }
  @keyframes sd-float-c {
    0%, 100% { transform: translateY(0px); }
    50%       { transform: translateY(-12px); }
  }
  @keyframes sd-float-d {
    0%, 100% { transform: translateY(0px); }
    50%       { transform: translateY(-8px); }
  }
  .sd-float-1 { animation: sd-float-a 3.5s ease-in-out infinite; }
  .sd-float-2 { animation: sd-float-b 4.2s ease-in-out infinite 0.6s; }
  .sd-float-3 { animation: sd-float-c 3.8s ease-in-out infinite 1.2s; }
  .sd-float-4 { animation: sd-float-d 4.6s ease-in-out infinite 0.3s; }

  #cards-cursor-zone,
  #cards-cursor-zone * { cursor: none; }
</style>

<script>
  const zone   = document.getElementById("cards-cursor-zone");
  const cursor = document.getElementById("cards-cursor");

  if (zone && cursor) {
    let visible = false;
    let rafId: number;
    let mouseX = 0, mouseY = 0;

    const move = (e: MouseEvent) => {
      mouseX = e.clientX;
      mouseY = e.clientY;
      cancelAnimationFrame(rafId);
      rafId = requestAnimationFrame(() => {
        cursor.style.left = mouseX + "px";
        cursor.style.top  = mouseY + "px";
      });
    };

    zone.addEventListener("mouseenter", () => {
      visible = true;
      cursor.style.opacity = "1";
    });
    zone.addEventListener("mouseleave", () => {
      visible = false;
      cursor.style.opacity = "0";
    });
    window.addEventListener("mousemove", move);
  }
</script>
