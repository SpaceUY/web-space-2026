---
import type { CaseStudy } from "../../data/case-studies";

interface Props {
  caseStudy: CaseStudy;
  /** Wide card spanning 2 columns */
  featured?: boolean;
  /** Dark page background — adjusts text colors */
  dark?: boolean;
  class?: string;
}

const { caseStudy, featured = false, dark = false, class: className = "" } = Astro.props;

const accentGradients: Record<NonNullable<CaseStudy["accent"]>, string> = {
  purple: "from-sd-purple to-sd-purple-2",
  cyan:   "from-sd-purple-300 to-sd-purple",
  green:  "from-sd-green to-sd-purple-300",
  mixed:  "from-sd-purple via-sd-purple-400 to-sd-green",
};

const gradient = accentGradients[caseStudy.accent ?? "purple"];

// Shared tag classes
const tagCategory = dark
  ? "bg-white/10 text-white/60 border-white/10"
  : "bg-sd-light-3 text-sd-text-gray border-sd-gray-border";
const tagService = dark
  ? "bg-white/[0.05] text-white/50 border-white/10"
  : "bg-white text-sd-text-gray border-sd-gray-border";
const textTitle  = dark ? "text-white"      : "text-sd-text-dark";
const textBody   = dark ? "text-white/55"   : "text-sd-text-gray";
const textLink   = dark
  ? "text-white/60 group-hover:text-sd-purple-300"
  : "text-sd-text-gray group-hover:text-sd-purple";
---

{featured ? (

  <!-- ── FEATURED — 2-col wide, same visual language as regular but bigger ── -->
  <a
    href={`/case-study/${caseStudy.slug}`}
    class={`group col-span-2 block cursor-pointer ${className}`.trim()}
    data-category={caseStudy.industry ?? caseStudy.category}
    data-animate
  >
    <!-- Wide image -->
    <div class="overflow-hidden rounded-xl mb-5 aspect-[16/9]">
      {caseStudy.image ? (
        <img
          src={caseStudy.image}
          alt={caseStudy.name}
          loading="lazy"
          class="w-full h-full object-cover transition-transform duration-[1400ms] ease-out group-hover:scale-105"
        />
      ) : (
        <div class={`w-full h-full bg-gradient-to-br ${gradient}`}></div>
      )}
    </div>

    <!-- Tags -->
    <div class="flex flex-wrap gap-2 mb-4">
      <span class={`inline-flex items-center px-2.5 py-1 rounded-md text-[10px] uppercase tracking-wider border ${tagCategory}`}>
        {caseStudy.category}
      </span>
      {caseStudy.services?.slice(0, 3).map((s) => (
        <span class={`inline-flex items-center px-2.5 py-1 rounded-md text-[10px] border ${tagService}`}>
          {s}
        </span>
      ))}
    </div>

    <!-- Name + tagline -->
    <h3 class={`text-2xl sm:text-3xl font-display font-semibold leading-tight mb-3 ${textTitle}`}>
      {caseStudy.name}
    </h3>
    <p class={`text-sm leading-relaxed mb-5 line-clamp-2 ${textBody}`}>
      {caseStudy.tagline}
    </p>

    <!-- Hover text link -->
    <span class={`inline-flex items-center gap-1.5 text-sm transition-all duration-200 group-hover:font-semibold ${textLink}`}>
      View case study
      <svg width="12" height="12" viewBox="0 0 14 14" aria-hidden="true" class="opacity-0 group-hover:opacity-100 transition-opacity duration-200">
        <path d="M3 7h8m0 0L7.5 3.5M11 7L7.5 10.5" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </span>
  </a>

) : (

  <!-- ── REGULAR card — image + content below ── -->
  <a
    href={`/case-study/${caseStudy.slug}`}
    class={`group block cursor-pointer ${className}`.trim()}
    data-category={caseStudy.industry ?? caseStudy.category}
    data-animate
  >
    <!-- Image — zooms on card hover -->
    <div class="overflow-hidden rounded-xl mb-5 aspect-[4/3]">
      {caseStudy.image ? (
        <img
          src={caseStudy.image}
          alt={caseStudy.name}
          loading="lazy"
          class="w-full h-full object-cover transition-transform duration-1000 ease-out group-hover:scale-105"
        />
      ) : (
        <div class={`w-full h-full bg-gradient-to-br ${gradient}`}></div>
      )}
    </div>

    <!-- Tags -->
    <div class="flex flex-wrap gap-2 mb-3">
      <span class={`inline-flex items-center px-2.5 py-1 rounded-md text-[10px] uppercase tracking-wider border ${tagCategory}`}>
        {caseStudy.category}
      </span>
      {caseStudy.services?.slice(0, 2).map((s) => (
        <span class={`inline-flex items-center px-2.5 py-1 rounded-md text-[10px] border ${tagService}`}>
          {s}
        </span>
      ))}
    </div>

    <!-- Name + tagline -->
    <h3 class={`text-xl font-display font-semibold leading-snug mb-3 ${textTitle}`}>
      {caseStudy.name}
    </h3>
    <p class={`text-sm leading-relaxed mb-4 line-clamp-2 ${textBody}`}>
      {caseStudy.tagline}
    </p>

    <!-- Hover text link -->
    <span class={`inline-flex items-center gap-1.5 text-sm transition-all duration-200 group-hover:font-semibold ${textLink}`}>
      View case study
      <svg width="12" height="12" viewBox="0 0 14 14" aria-hidden="true" class="opacity-0 group-hover:opacity-100 transition-opacity duration-200">
        <path d="M3 7h8m0 0L7.5 3.5M11 7L7.5 10.5" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </span>
  </a>

)}
